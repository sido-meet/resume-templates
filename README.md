# Resume Templates

收集各种简历模板(HTML / LaTeX / Markdown / 其他)。

目前为空,后续按需加入。

## 添加模板的约定(以后)

- 每个模板一个子目录(如 `vibe-resume-upstream/`、`latexcv-zh/`、`moderncv/`)
- 子目录里放:模板源码 + 自己的 README(标注 upstream URL、snapshot 日期、用途)
- 纯模板参考,**不放个人简历内容**(姓名/邮箱/项目细节)
